server:
  servlet:
    # Prefix all endpoints with /api
    context-path: /api

spring:
  datasource:
    url: "jdbc:h2:mem:testdb"
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

data:
  recipes-file: "file:src/main/resources/data/recipes.test.json"
